#include "main.h"

/**
 * _strlen - counts the length of a string
 * @s: the input string
 *
 * Return: the number of characters in the string
 */
int _strlen(char *s)
{
	int count = 0;

	for (; s[count]; count++)
	{
		;
	}

	return (count);
}
